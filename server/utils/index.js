// Authorization
const Authorization = require('./auth')

// Password Module
const Password = require('./password')

// SendError Module
const { SendError } = require('./sendError')

// Export Utilities
module.exports = { Authorization, Password, SendError }